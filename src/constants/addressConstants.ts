import { PartialBaseAddress } from '../types/types.js';

export const addressInputs: PartialBaseAddress = {
  Country: 'country',
  City: 'city',
  Street: 'streetName',
  'Postal code': 'postalCode',
};
